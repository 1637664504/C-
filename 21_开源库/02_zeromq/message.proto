syntax = "proto3"; // 声明了protobuf的版本
package UserMessage;
message Person
{
    string name = 1;    // 1表示第一个字段
    int32 id = 2;
    string email = 3;
}

enum PhoneType
{
    MOBILE = 0;
    HOME = 1;
    WORK = 2;
}

message PhoneNumber
{
    string number = 1;
    PhoneType type = 2;
}
