<busconfig>

  <policy user="root">
    <allow own="test.signal.sink"/>
    <allow own="test.signal.source"/>
    <allow own="test.method.server"/>
    <allow own="test.method.caller"/>
  </policy>

  <policy user="liuj">
    <allow own="test.signal.sink"/>
    <allow own="test.signal.source"/>
    <allow own="test.method.server"/>
    <allow own="test.method.caller"/>
  </policy>

  <policy context="default">
    <allow send_interface="test.signal.Type"/>
    <allow send_interface="test.method.Type"/>
  </policy>

</busconfig>
